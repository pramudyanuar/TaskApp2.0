// /** @type {import('next').NextConfig} */
// const nextConfig = {}

// module.exports = nextConfig


// next.config.js

const nextConfig = {
  images: {
    domains: ['upload.wikimedia.org'],
  },
};

module.exports = nextConfig;
